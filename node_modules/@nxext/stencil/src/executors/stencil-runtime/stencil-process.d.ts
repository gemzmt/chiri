import { PathCollection } from './types';
import type { Config } from '@stencil/core/compiler';
export declare function createStencilProcess(config: Config, pathCollection: PathCollection): Promise<{
    success: boolean;
    error?: undefined;
} | {
    success: boolean;
    error: any;
}>;
