#!/usr/bin/env node
"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const path_1 = __importDefault(require("path"));
const __1 = require("..");
const log_1 = require("../helpers/log");
const { name } = require(path_1.default.resolve(process.cwd(), 'package.json'));
const command = process.argv[2];
if (!command) {
    log_1.logError(`You need to specify a valid command. Use "${name} help" for more info`);
}
switch (command) {
    case 'publish':
        __1.publish();
        break;
    case 'info':
        const packageName = process.argv[3];
        __1.info(packageName);
        break;
    case 'help':
        log_1.logInfo('Cornershop S3 npm');
        log_1.logInfo(`@cornershop/cs-npm-s3 publish: publish the package in which this script is run`);
        log_1.logInfo(`@cornershop/cs-npm-s3 info <package_name>: retrieve the info of the package. It should start with @cornershop/`);
        break;
    default:
        log_1.logError(`"${command}" is not a valid command. Use "${name} help" for more info`);
        break;
}
//# sourceMappingURL=npm-s3.js.map